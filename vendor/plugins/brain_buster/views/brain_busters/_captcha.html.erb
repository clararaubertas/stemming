
	<span id="brain_buster_captcha" class="<%= if last_captcha_attempt_failed? then 'captcha_failed' end %>">
	
		<%= hidden_field_tag :captcha_id, @captcha.id %>
		
		<label for="captcha_answer">
		<strong>Don't be a robot!</strong>
		</label><br />
		
	 <%= @captcha.question %><br />	<%= text_field_tag :captcha_answer, params[:captcha_answer], :size => 10, :maxlength => 40 %></td>
		
	</span>

